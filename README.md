# 🎬 Video Downloader Pro
### موقع تحميل الفيديوهات المتكامل

موقع ويب متكامل لتحميل الفيديوهات والصوت من يوتيوب ومنصات متعددة باستخدام أداة yt-dlp القوية في الخلفية.

## ✨ الميزات الكاملة

### 📥 تحميل متعدد الصيغ
- **تحميل الفيديو**: دعم جميع الجودات من 144p حتى 4K
- **تحويل إلى MP3**: استخراج الصوت وحفظه كملف صوتي عالي الجودة
- **تحميل القوائم**: تحميل قوائم تشغيل كاملة دفعة واحدة

### 🎨 واجهة مستخدم متقدمة
- **ثيم داكن/فاتح**: تبديل تلقائي أو يدوي بين الثيمات
- **تصميم متجاوب**: يعمل بشكل مثالي على جميع الأجهزة
- **دعم كامل للعربية**: اتجاه RTL مع خطوط Cairo الجميلة

### 📊 أدوات المراقبة والتحليل
- **تتبع التقدم**: شريط تقدم تفاعلي مع عرض السرعة والوقت المتبقي
- **رسوم بيانية**: رسوم بيانية لحالة التحميلات (Chart.js)
- **إحصائيات**: عرض إجمالي التحميلات والتحميلات النشطة

### 🔔 نظام الإشعارات
- **إشعارات المتصفح**: تنبيهات عند اكتمال التحميل
- **إشعارات داخل الصفحة**: رسائل toast جميلة

### 📜 سجل التحميلات
- **حفظ السجل**: تخزين تاريخ التحميلات
- **إعادة الاستخدام**: إعادة تحميل نفس الفيديو بنقرة واحدة
- **حذف السجل**: إمكانية مسح السجل

### 🔗 ميزات إضافية
- **نسخ الروابط**: زر لنسخ روابط التحميل
- **لصق من الحافظة**: دعم كامل للوحة المفاتيح
- **إلغاء التحميل**: إمكانية إلغاء أي تحميل في أي وقت

## 🚀 طريقة التثبيت

### المتطلبات الأساسية

- **Python 3.8 أو أحدث**
- **FFmpeg**: مطلوب لدمج الصوت مع الفيديو
- **Git**: للتحميل المشروع

### خطوات التثبيت

```bash
# 1. استنساخ المشروع
git clone https://github.com/yourusername/video-downloader.git
cd video-downloader-complete

# 2. إنشاء بيئة افتراضية (اختياري)
python -m venv venv
# Windows
venv\Scripts\activate
# Linux/Mac
source venv/bin/activate

# 3. تثبيت المتطلبات
pip install -r requirements.txt

# 4. تثبيت FFmpeg
# Ubuntu/Debian:
sudo apt update && sudo apt install ffmpeg

# macOS:
brew install ffmpeg

# Windows:
# حمل من https://www.gyan.dev/ffmpeg/builds/
# واستخرج وأضف المسار إلى PATH

# 5. تشغيل الخادم
python app.py

# 6. فتح الموقع
# افتح المتصفح على http://localhost:5000
```

## 📖 طريقة الاستخدام

### تحميل فيديو عادي

1. انسخ رابط الفيديو من يوتيوب أو أي منصة مدعومة
2. الصق الرابط في حقل الإدخال
3. اختر "فيديو" أو "صوت فقط (MP3)"
4. انقر "تحليل الرابط"
5. اختر الجودة المطلوبة
6. انقر "تحميل الفيديو"

### تحميل قائمة تشغيل

1. انسخ رابط قائمة التشغيل
2. الصق الرابط في حقل الإدخال
3. انقر "تحليل الرابط" - سيظهر عدد الفيديوهات
4. اختر الجودة
5. انقر "تحميل" - سيتم تحميل جميع الفيديوهات

### تحويل إلى MP3

1. الصق رابط الفيديو
2. اختر "صوت فقط (MP3)" من الخيارات
3. انقر "تحليل الرابط"
4. اختر الجودة (أفضل جودة 320k)
5. انقر "تحويل إلى MP3"

## 🌐 المنصات المدعومة

| المنصة | دعم الفيديو | دعم الصوت | دعم القوائم |
|--------|-------------|-----------|-------------|
| YouTube | ✅ | ✅ | ✅ |
| TikTok | ✅ | ✅ | ❌ |
| Instagram | ✅ | ❌ | ❌ |
| Facebook | ✅ | ✅ | ❌ |
| Twitter/X | ✅ | ✅ | ❌ |
| Pinterest | ✅ | ❌ | ❌ |
| Vimeo | ✅ | ✅ | ❌ |
| DailyMotion | ✅ | ✅ | ❌ |

## 🏗️ هيكل المشروع

```
video-downloader-complete/
├── app.py                  # الخادم الخلفي (Flask + Socket.IO)
├── requirements.txt        # متطلبات Python
├── README.md              # هذا الملف
├── downloads/             # مجلد التحميلات النهائية (يُنشأ تلقائياً)
├── temp/                  # مجلد الملفات المؤقتة (يُنشأ تلقائياً)
└── templates/
    └── index.html         # واجهة المستخدم المتكاملة
```

## 🔧 مسارات API

| المسار | الطريقة | الوصف |
|--------|---------|-------|
| `/` | GET | الصفحة الرئيسية |
| `/api/info` | POST | تحليل رابط الفيديو |
| `/api/download` | POST | بدء تحميل الفيديو |
| `/api/status/<task_id>` | GET | الحصول على حالة التحميل |
| `/download/<task_id>` | GET | تنزيل الملف المكتمل |
| `/api/cancel/<task_id>` | GET | إلغاء التحميل |
| `/api/history` | GET | جلب سجل التحميلات |
| `/api/history` | DELETE | حذف السجل |
| `/api/queue` | GET | الحصول على قائمة الانتظار |

## ⚙️ الإعدادات

### تغيير المنفذ

في ملف `app.py`:

```python
socketio.run(app, debug=True, host='0.0.0.0', port=5000)
# غير 5000 إلى المنفذ المطلوب
```

### تغيير وقت حذف الملفات

```python
FILE_EXPIRY_HOURS = 2  # يُحذف الملف بعد ساعتين
```

### تغيير عدد التحميلات المتزامنة

```python
MAX_CONCURRENT_DOWNLOADS = 3
```

## 🚀 النشر على استضافة

### باستخدام Docker

```dockerfile
FROM python:3.9-slim

WORKDIR /app
COPY . .

RUN apt-get update && apt-get install -y ffmpeg
RUN pip install -r requirements.txt

EXPOSE 5000

CMD ["python", "app.py"]
```

```bash
# بناء وتشغيل
docker build -t video-downloader .
docker run -p 5000:5000 video-downloader
```

### على VPS (DigitalOcean/AWS)

1. أنشئ سيرفر Ubuntu 20.04+
2. ثبت Python و Git و FFmpeg
3. انسخ المشروع
4. ثبت المتطلبات
5. استخدم gunicorn لتشغيل الخادم:

```bash
pip install gunicorn
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## ⚠️ ملاحظات مهمة

### القيود الفنية

- قد يتم حظر عنوان IP عند التحميل المكثف
- بعض الفيديوهات قد تكون محمية ضد التحميل
- الجودات العالية تتطلب وقت ومساحة أكبر

### النصائح

1. **للاستخدام الشخصي**: شغل محلياً على جهازك
2. **للاستخدام العام**: استخدم VPS مع عنوان IP مخصص
3. **المراقبة**: راقب مساحة القرص بانتظام
4. **التحديث**: حدث yt-dlp بانتظام

### الشروط القانونية

- استخدم فقط لتحميل المحتوى الذي لديك حق تحميله
- احترم حقوق الملكية الفكرية
- الموقع غير مسؤول عن الاستخدام

## 🙏 الشكر والتقدير

- [yt-dlp](https://github.com/yt-dlp/yt-dlp) - أداة التحميل القوية
- [Flask](https://flask.palletsprojects.com/) - إطار عمل Python
- [Chart.js](https://www.chartjs.org/) - الرسوم البيانية
- [Socket.IO](https://socket.io/) - الاتصال في الوقت الفعلي

---

**ملاحظة**: هذا مشروع للأغراض التعليمية. استخدمه بمسؤولية واحترم حقوق المبدعين.

⭐ إذا أعجبك المشروع، لا تنس وضع نجمة على GitHub!
